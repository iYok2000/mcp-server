# Required: OpenAI API key (research/review tools)
OPENAI_API_KEY=

# Optional: override paths for deploy (Docker, CI, different install dir)
# Default: repo-relative (control/mcp.db, core/target/release/core)
# MCP_DB_PATH=/data/mcp.db
# MCP_CORE_BIN=/app/core
